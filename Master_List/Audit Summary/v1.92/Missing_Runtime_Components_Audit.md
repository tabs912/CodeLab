# Missing Runtime Components Audit — v1.92

**Authoritative source:** `Master_List/Current Production Script/v1.92 Module.txt`
**Source SHA-256:** `8eb52f65ffda8520d769f6cf588c8482bfa897af9296bce41b6512591f227ab2`
**Method:** focused static review of the 4,455-line assembled script as one Apps Script project, plus manifest scope review. Passing components are intentionally omitted.
**Runtime limit:** spreadsheet contents, deployed triggers, document properties, file access, and authorizations were not available for live verification.

| ID | Severity | Missing or Invalid Item | Type | Referenced By | File/Module | Evidence | Runtime Impact | Recommended Fix |
|---|---|---|---|---|---|---|---|---|
| MRC-001 | **Critical** | `BANNER_PREFIX`, `CARE_PLAN_DUE_PREFIX`, and `UNLOCKED_PREFIX` initialize by reading themselves inside their own `const` temporal dead zones | Global constants | `FORMATTER_ROUTES`; transitively every project entry point because globals evaluate at load | `v1.92 Module.txt`, Module 5, lines 2594–2602 | Each declaration has the form `const X = typeof X !== "undefined" ? X : default`. For a lexical binding, even `typeof X` throws before `X` is initialized. | The assembled project can fail during global evaluation before `onOpen`, menus, or any workflow can run. | Replace the three self-references with direct defaults, or read overrides from a differently named configuration object/property before declaring these constants. Validate by loading the complete Apps Script project after the change. |
| MRC-002 | **Critical** | `quickSystemSetup` is undefined | Menu callback | “🏗 System Set up” | Module 1, line 57 | `onOpen` registers the exact callback string, but no global function with that name exists among the 207 unique function definitions. | The menu item cannot begin and produces a “function not found” failure when selected. | Add a global `quickSystemSetup` implementation or replace the registration with the exact name of the approved existing setup workflow. |
| MRC-003 | **Critical** | `quickBuildAllTemplates` is undefined | Menu callback | “🖼 Build Templates + Validate Templates” | Module 1, line 58 | The callback string is registered but no matching global declaration exists. | The Quick Start-up template-build item cannot begin. | Add the missing global wrapper and have it call the approved template build and validation functions, or register the approved existing callback name. |
| MRC-004 | **Critical** | `runDashboardQualityWorkflow` is undefined | Menu callback | “✅ Dashboard Quality Workflow” and Maintenance/Rebuild → Quality → “Dashboard Quality Workflow” | Module 1, lines 59 and 63 | Two menu items register the same absent global callback. | Both dashboard-quality workflow menu items fail immediately. | Restore a global `runDashboardQualityWorkflow` implementation or update both registrations to the approved available workflow function. |
| MRC-005 | **Critical** | `runDashboardQualityStartUp` is undefined | Menu callback | Maintenance/Rebuild → Quality → “Dashboard Quality Start up” | Module 1, line 62 | The callback is registered but never declared. The similarly named `dummyDashboardQualityStartUp` is not the registered callback and does not establish equivalence. | The dashboard-quality start-up menu item cannot begin. | Restore the exact global callback or deliberately register an approved implemented replacement. Do not infer replacement from the dummy function name. |
| MRC-006 | **Critical** | `createOrRefreshAllReportTemplates` is undefined | Menu callback | Start-up → “🖼 Create / Refresh All Templates” | Module 1, line 79 | The registered callback does not exist. `createAllReportTemplates` exists at line 1642 but is neither registered nor a complete name match. | The principal template creation/refresh menu item cannot begin. | Add the missing global orchestration wrapper, including base/system/operational template steps and validation, or register an explicitly approved existing implementation. |
| MRC-007 | **High** | `getTemplateConfigFromDashboard_` is undefined | Configuration helper | `buildFrameworkTimingTemplate_`, `buildDashboardQualityTemplate_`, `buildIndexTemplate_`, and `buildOperationalReportTemplate_` | Module 3, lines 1638–1655 | Four unguarded calls reference a function that is never declared. | System and operational report-template builders stop with `ReferenceError`; all eight operational builders are affected through the shared wrapper. | Add `getTemplateConfigFromDashboard_` with a validated configuration return contract, or replace every call with the approved existing dashboard loader/adapter. |
| MRC-008 | **High** | `writeTemplateMetadata_` is undefined | Template metadata helper | `buildGeneralTemplate_` | Module 3, line 1385 | The call is unconditional and no declaration exists. | Any path that reaches general template construction stops after partially creating and formatting a sheet, leaving incomplete template state. | Implement the metadata writer with the expected `(sheet, config, targetCols)` signature, or remove/replace the call only if metadata is explicitly no longer required. Add cleanup for partially built templates on failure. |
| MRC-009 | **High** | Duplicate month-prompt and month-context globals expose incompatible return contracts | Conflicting globals / return contract | `runFormatterPipeline_`, all four hide/archive callbacks, `createMasterList`, and `buildMonthlyChangeReport` | Modules 1–2, lines 551–579 and 1185–1211; consumers at 849–912, 2616–2640, and 3852–3881 | The first `promptForLockedYearReportMonth_` returns a context object, while the later declaration replaces it and returns raw text. The later `buildPromptedMonthContext_` also omits `label`, `mm`, and `yy` supplied by the first definition. Some callers reparse the result; formatter, Master List, and Monthly Change callers treat it directly as a context object. | Selected-month workflows can silently use the current month, select the wrong source sheet, create incorrectly named output, or pass undefined dates into downstream processing. | Keep one global prompt function with one documented object return contract. Remove the duplicate definitions, restore `monthLabel`/`label`/`mm`/`yy`/date fields, and update every caller to consume that single contract consistently. |
| MRC-010 | **High** | Archive connection helper is absent while local source deletion can still proceed | Missing helper / destructive control-flow guard | `runFormatterPipeline_` → `processSingleSubReport_` | Module 5, lines 2623–2626 and 2754–2763 | `openArchiveSpreadsheetOnce_` is undefined, so the guarded expression always yields `null`. Archiving is then skipped because `archiveSs` is falsey, but Banners, CP Due, and Unlock CP routes still call `deleteSheetSafely_` when `route.deleteLocal` is true. | A formatting run can delete local imported source sheets without first creating an archive copy. | Implement and validate `openArchiveSpreadsheetOnce_`; more importantly, make local deletion conditional on a confirmed successful archive result whenever auto-archive is enabled. Stop with a controlled error if archive access or copy confirmation is unavailable. |
| MRC-011 | **Not Verified** | Archive spreadsheet ID and access | Document property / external spreadsheet | `archiveMonthlySheetsBySpecs_` and archive configuration workflow | Module 2, lines 1047–1054; Module 4, lines 2561–2578 | Monthly archive falls back to hard-coded ID `1PEEoXzPG-xRFuqDW_ZjPzyqdTUd_5AOwx0nbbzmMwBc`; source review cannot confirm that the file exists, is the correct archive, or is accessible to the executing user. | `SpreadsheetApp.openById` can halt archive workflows before any per-sheet error handling begins. | Initialize and validate `RFF_ARCHIVE_SPREADSHEET_ID` during setup, remove or formally approve the hard-coded fallback, and perform an access check before starting the batch. |
| MRC-012 | **Not Verified** | Required source, dashboard, base-template, and report-template sheets | Worksheet/template runtime dependencies | Template creation, active-system-sheet creation, monthly formatting, Master List, and Monthly Change workflows | Modules 1–7; explicit blocking check at lines 2688–2695 | The source includes fallbacks and some controlled null handling, but the actual workbook sheet population was not supplied. Missing both the route template and `RFF_BASE_TEMPLATE` causes `processSingleSubReport_` to throw. Missing menu/template helpers above also prevent normal regeneration paths. | Workflows can stop on missing templates or return without creating required system sheets. | After correcting MRC-001–MRC-010, run setup against the target workbook; create and validate `Format Dashboard`, `RFF_BASE_TEMPLATE`, all required `Template - …` sheets, Index, timing, and quality surfaces before enabling production menus. |

## Final Summary

- **Total Critical findings:** 6
- **Total High findings:** 4
- **Total Medium findings:** 0
- **Total Not Verified findings:** 2
- **Overall status:** `FAIL` — global initialization can fail, five registered menu callback names are missing, and confirmed shared dependency/contract defects can halt or misdirect production workflows.
