# Module 3 v2.0.11 Release Notes

- Applies the configured governance rank note to `B1` on `RFF_BASE_TEMPLATE`.
- Applies the configured governance rank note to `B1` on every general and operational template after final template sizing and positioning.
- Requires Module 2 v2.16 or later.
