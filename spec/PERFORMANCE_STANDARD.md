# Performance Standard

Review for:

- Service calls in loops
- Repeated reads and writes
- Repeated formatting
- Row-by-row deletion
- O(n²) lookups
- Unnecessary flush calls
- Repeated parsing
- Large full-sheet operations
- Excessive logging

Prefer arrays, Maps, Sets, cached references, and batch operations.
