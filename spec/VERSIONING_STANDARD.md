# Versioning Standard

Use:

- `vX` for production milestone
- `vX.XX` for major change
- `vX.XX.XX` for minor change, correction, cleanup, or performance update

Every generated production script receives a new version.

Never overwrite a previous production version.
