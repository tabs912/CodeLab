# CodeLab Prompt Library

Choose a prompt based on the task.

The prompt defines the objective.

`README.md`, `AGENTS.md`, root `spec/`, and project-specific specifications define how the task is performed.
