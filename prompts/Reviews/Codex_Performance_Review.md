Review the active script or project for Google Apps Script performance.

Focus on service calls, loop behavior, full-sheet reads, repeated writes, formatting, deletion, sorting, parsing, logging, quotas, memory, and time complexity.

Provide prioritized optimizations without changing business logic.
