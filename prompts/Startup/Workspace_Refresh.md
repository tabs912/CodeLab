Refresh the current workspace safely.

Run:

```bash
./tools/sync_workspace.sh
```

Then search again for the requested repository artifact.

Do not use reset, clean, force checkout, or branch switching.
