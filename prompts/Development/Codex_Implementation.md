Run the CodeLab startup procedure.

Implement only approved recommendations.

Preserve business logic, increment the version, replace complete affected functions, update callers and helpers, remove obsolete code when safe, preserve compatibility, return complete files, and include release notes and tests.
