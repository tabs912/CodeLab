# Repository Standard

The repository is a shared engineering workspace.

Repository-wide standards live in `Framework/`.

Project-specific rules live inside project folders.

`Archive_To_Move/` is excluded from analysis unless explicitly requested.

Binary reports may be stored in project `Reports/` folders.
