# Versioning Standard

- `vX` — production milestone
- `vX.XX` — major change
- `vX.XX.XX` — minor change, correction, cleanup, or optimization

Every generated production script receives a new version.

Never overwrite an earlier production version.
